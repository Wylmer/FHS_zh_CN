## /tmp：临时文件

### 3.17.1. 用途

`/tmp`文件夹必须能够为要求临时文件的程序使用。

程序必须假设`/tmp`中的任何文件或文件夹仅用于在程序中引用。

> #### 基础知识

> IEEE标准 P1003.2（POSIX，第2部分）做出了与上节相似的要求。

> 尽管`/tmp`中的文件可以站点指定的方式保存，我们依然建议在每次系统启动时删除`/tmp`下的文件和文件夹。

> FHS基于历史先例和习惯做法增加了这条建议，但不作为要求，因为系统管理不在此标准讨论的范围内。
